<h1>Listing micropostcorrects</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @micropostcorrects.each do |micropostcorrect| %>
      <tr>
        <td><%= micropostcorrect.content %></td>
        <td><%= micropostcorrect.user_id %></td>
        <td><%= link_to 'Show', micropostcorrect %></td>
        <td><%= link_to 'Edit', edit_micropostcorrect_path(micropostcorrect) %></td>
        <td><%= link_to 'Destroy', micropostcorrect, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Micropostcorrect', new_micropostcorrect_path %>
